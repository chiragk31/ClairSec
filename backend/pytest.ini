[pytest]
asyncio_mode = strict
markers =
    docker: mark a test as requiring a running Docker daemon (deselect with -m "not docker")
    llm: mark a test as requiring external LLM API access (deselect with -m "not llm")
